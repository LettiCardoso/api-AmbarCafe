{
  "name": "api-ambarcafe",
  "version": "1.0.0",
  "private": true,
  "scripts": {
   "start": "npx json-server --watch db.json --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}